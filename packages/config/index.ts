// Don't export eslint config from main entry - it should only be used in eslint.config.ts files
// If you need it, import directly: import { eslintConfig } from "@growserver/config/eslint"
// export * from "./eslint";
export * from "./config";
